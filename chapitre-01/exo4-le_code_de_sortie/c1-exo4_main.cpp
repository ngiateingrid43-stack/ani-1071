#include <iostream>
int main(int argc, char* argv[]) {
    // Affichage de mon nom sur une ligne
    std::cout << "NGIATE KAMNANG INGRID\n";
    // Affichage de ma ville sur une ligne
    std::cout << "YAOUNDE";
    return 3;
}
/*Le code de sortie est la valeur numérique qu'un programme renvoie au système à sa terminaison, 
transmise indépendamment de tout texte affiché à l'écran. Par convention, 0 signale un succès et toute autre valeur signale un échec 
dans notre cas c'est 3 et ca peut signifier une erreur */